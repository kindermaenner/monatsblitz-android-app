# 7. Verteilungssicht
Die Anwendung läuft als monolithische APK auf Android-Endgeräten. Die Kommunikation mit dem WordPress-Server erfolgt verschlüsselt via HTTPS und einem API-Key Header (`X-MB-Key`).
